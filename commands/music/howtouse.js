const {
    Message,
    Client
} = require("discord.js");

module.exports = {
    name: "howtouse",
    aliases: [],
    description: "Music available using Slah Commands `/`",
    /**
     *
     * @param {Client} client
     * @param {Message} message
     * @param {String[]} args
     */
    run: async (client, message, args) => {
        
    },
};